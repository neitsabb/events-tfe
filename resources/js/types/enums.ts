export enum EventStepName {
    'GENERAL' = 'Général',
    'DATE' = 'Date',
    'LOCATION' = 'Location',
    'TICKETS' = 'Tickets',
    'RESUME' = 'Résumé',
}

export enum EventStatus {
    DRAFT = 'draft',
    PUBLISHED = 'published',
    ARCHIVED = 'archived',
    NOT_CONFIGURED = 'not_configured',
}
